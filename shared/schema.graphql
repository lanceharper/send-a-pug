schema {
  query: Query
  mutation: Mutation
}

type Jerk {
  title: String!
}

type Mutation {
  createJerk: Jerk
}

type Foo {
  bar: String!
  sentAt: String!
}

type Query {
  getNone(foo: String): Foo
}
