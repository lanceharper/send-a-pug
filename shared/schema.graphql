schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}

type Subscription {
  inbox(to: String!): Jerk @aws_subscribe(mutations: ["createJerk"])
}

type Jerk {
  from: String!
  to: String!
  message: String!
  sentAt: String!
}

type Mutation {
  createJerk(message: String!, to: String): Jerk!
}

type Query {
  me: String
}
